<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.SureTouchAPP">

        <receiver
            android:name=".ui.screens.notifications.ClassScheduleAlarmReceiver"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SureTouchAPP"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep-link callbacks for LinkedIn and GitHub OAuth -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="suretrust"
                    android:host="oauth" />
                <data
                    android:scheme="suretrust"
                    android:host="linkedin-oauth" />
                <data
                    android:scheme="suretrust"
                    android:host="linkedin" />
                <data
                    android:scheme="suretrust"
                    android:host="github-oauth" />
            </intent-filter>

            <!-- HTTP & HTTPS Deep-link callbacks for server OAuth redirects -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="sureproed.com" />
                <data android:scheme="http" android:host="sureproed.com" />
                <data android:scheme="https" android:host="api.sureproed.com" />
                <data android:scheme="http" android:host="api.sureproed.com" />
                <data android:scheme="http" android:host="106.51.129.34" android:port="8000" />
                <data android:scheme="http" android:host="106.51.129.34" />
                <data android:scheme="https" android:host="106.51.129.34" />
                <data android:scheme="http" android:host="suretrust.com" />
                <data android:scheme="https" android:host="suretrust.com" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
